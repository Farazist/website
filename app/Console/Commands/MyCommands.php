<?php

	// $con = mysqli_connect("localhost", "farazist_user", "sara&sajjad&amir", "farazist_db");
	
	// mysqli_query($con, "UPDATE systems SET state='active' 
	// 					WHERE activation_type='auto'
	// 					AND ((HOUR(NOW()) BETWEEN morning_start_time AND morning_end_time - 1)
	// 					OR (HOUR(NOW()) BETWEEN afternoon_start_time AND afternoon_end_time - 1))");

	// mysqli_query($con, "UPDATE systems SET state='deactive' 
	// 					WHERE activation_type='auto' 
	// 					AND ((HOUR(NOW()) NOT BETWEEN morning_start_time AND morning_end_time - 1)
	// 					AND (HOUR(NOW()) NOT BETWEEN afternoon_start_time AND afternoon_end_time - 1))");

?>